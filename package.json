{
  "name": "@tokenring-ai/acp",
  "version": "0.2.0",
  "description": "Agent Client Protocol (ACP) integration for TokenRing agents, enabling stdio-based communication with ACP-compatible clients.",
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.18.0",
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/app": "workspace:*",
    "@tokenring-ai/filesystem": "workspace:*",
    "@tokenring-ai/terminal": "workspace:*",
    "zod": "^4.3.6"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  },
  "type": "module"
}
